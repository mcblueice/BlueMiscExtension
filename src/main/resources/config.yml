Database:
  # type: sqlite, MySQL
  type: sqlite
  MySQL:
    host: localhost
    port: 3306
    database: database
    user: user
    password: password
Features:
  AbsorptionScale:
    enable: true
    max_amount: 20
  DamageIndicatorLimiter:
    enable: true
    max_amount: 20
  ShulkerBox:
    enable: true
    cooldown: 1000